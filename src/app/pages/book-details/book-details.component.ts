import { Component } from '@angular/core';

@Component({
  selector: 'app-book-details-page',
})
export class BookDetailsPageComponent {
  /**
   * This component is a page & will display the details for one book.
   *
   * It will show the book cover image, title, author, genre, & if given: review & rating
   *
   * There will also be a button to add a review for the book that only shows if a book does not yet have a review.
   * When this button is clicked, a form dialog opens, allowing user to submit a review for the book.
   *
   */
}
